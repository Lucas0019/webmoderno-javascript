/**
 * Math é um objeto embutido que tem propriedades e métodos para constantes e funções matemáticas,
 * e NÃO é um objeto de função
 */
const raio = 9.2
const area = Math.PI * Math.pow(raio, 2)

console.log(area)
console.log(typeof Math)